apiVersion: rabbitmq.com/v1beta1
kind: Queue
metadata:
  name: ip-count
spec:
  name: ip_count # name of the queue
  autoDelete: false
  durable: true # seting 'durable' to false means this queue won't survive a server restart
  rabbitmqClusterReference:
    # note: the RabbitMQ cluster must exist in the same namespace as this resource
    name: ip-visit-rabbitmq-cluster
